# ── Internal scaffolding (not for public repo) ──
EXECUTOR_PROMPT.md
.remember/

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
# anchored to repo root only — must NOT ignore source pkg zipper_system/build/
/build/
/dist/
# PyInstaller installer output (the built .exe is large + reproducible via
# tools/build_installer.bat — keep it out of the public repo).
/installer/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# ── Maya ──
*.swatches
*.mayaSwatches
mayaPython.log
*.ma.swatches
incrementalSave/
*.bak

# ── OS / IDE ──
.DS_Store
Thumbs.db
.idea/
.vscode/
*.sublime-*
